module Enum

imports

    Common
    Type

context-free sorts

    Enum

context-free syntax

    Enum.Enum = <
        enum <ID>
        {
            <{ID ",\n"}*>
        }
    >

lexical syntax

    ID = "enum" {reject}
