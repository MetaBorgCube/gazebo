module gazebo/Alias

imports

    gazebo/Common
    gazebo/Selector

context-free sorts

    Alias

context-free syntax

    Alias.Alias = <alias <Selector> = <Selector>>
    Alias.GlobalAlias = <global alias <Selector> = <Selector>>
